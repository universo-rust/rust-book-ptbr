---
title: "Enums e pattern matching"
chapter_code: 06-00
slug: enums-e-pattern-matching
challenge_day: 7
reading_minutes: 1
---

# Enums e pattern matching

Neste capítulo, veremos enumerações, também chamadas de _enums_. Enums permitem definir um tipo enumerando suas variantes possíveis. Primeiro, definiremos e usaremos um enum para mostrar como um enum pode codificar significado junto com dados. Em seguida, exploraremos um enum particularmente útil, chamado `Option`, que expressa que um valor pode ser ou algo ou nada. Depois, veremos como o pattern matching na expressão `match` facilita executar código diferente para valores diferentes de um enum. Por fim, cobriremos como a construção `if let` é outro idioma conveniente e conciso disponível para lidar com enums no seu código.
